# 📚 カリキュラム再構成計画

**最終更新**: 2025-11-01

---

## 🎯 再構成の目的

### 現状の問題点

1. **知識ゼロでいきなり購入**
   - Week 1で100円購入 → 何も理解していない状態
   - リスク管理の知識なし

2. **固定カリキュラム**
   - ユーザーの状況に応じた調整ができない
   - 一方通行の学習

3. **手動データ管理**
   - 保有資産を手動でメモ
   - リアルタイム分析ができない

### 改善の方向性

```
【旧】知識ゼロ → いきなり購入 → 後から学習
【新】基礎知識 → デモ体験 → 少額実践 → 動的学習（Claudeと対話）
```

---

## 📋 新カリキュラム構成

### **Week 0: 基礎知識（買う前に学ぶ）** 🆕

**目標**: 最低限の知識を習得してから購入

#### 学習内容

1. **ローソク足の読み方**
   - 始値・終値・高値・安値
   - 陽線・陰線の意味
   - ヒゲの見方

2. **基本的なテクニカル指標**
   - RSI（相対力指数）: 買われすぎ・売られすぎ
   - MACD（移動平均収束拡散）: トレンドの方向
   - Bollinger Bands（ボリンジャーバンド）: ボラティリティ

3. **リスク管理の基本**
   - 余剰資金の範囲内
   - 損切りラインの設定
   - 分散投資の考え方

#### 実践演習

- Streamlit UIでチャートを見ながら学習
- 過去データで「もしこのタイミングで買っていたら？」シミュレーション

**所要時間**: 2-3時間

---

### **Week 1: 初めての購入（100円）**

**前提**: Week 0完了済み

#### 学習内容

1. **取引所の選択**
   - MEXC vs Binance vs Bybit
   - 各取引所の特徴

2. **API Key設定**
   - セキュリティ設定
   - 2FA問題の解決（API Key使用）
   - 取引制限の設定

3. **初めての購入**
   - 100円分のコイン購入
   - **自動データ記録開始**（手動メモ不要）

#### 実装機能

```python
# ポートフォリオトラッキング開始
tracker = PortfolioTracker(api_key, api_secret)
tracker.start_monitoring()  # 1分ごとに自動保存
```

**所要時間**: 1-2時間

---

### **Week 2-4: 動的カリキュラム（Claudeとの対話）** 🆕

**目標**: 保有データを見ながら、状況に応じた学習

#### Week 2: RSI分析の実践

**学習フロー**:

1. **自分の保有銘柄のRSI確認**
   ```
   あなたのSHIB: RSI 72（買われすぎ）
   市場平均: RSI 55
   ```

2. **Claude解説生成**（自動）
   ```markdown
   ## 📊 2025-11-01 18:00 現状分析

   あなたのSHIBはRSI 72で買われすぎゾーンです。
   一般的に、RSI > 70は調整の可能性が高まります。

   ### 推奨アクション
   - 様子見: RSIが60以下に戻るまで待つ
   - 一部利確: 購入価格の+10%で売却検討
   ```

3. **インタラクティブ学習**
   - UI上でRSIスライダーを操作
   - 「RSI 30の時に買っていたら？」シミュレーション

#### Week 3: MACD＋ボリンジャーバンド

**Week 2と同様の流れ**:
- 自分のデータ
- Claude解説
- UI操作で理解

#### Week 4: 統合分析

**複数指標を組み合わせた判断**:
- RSI + MACD + Bollinger Bands
- 総合スコアリング

---

### **Week 5-7: 高度な分析**

**既存カリキュラムを活用**:
- Week 5: ARIMA/GARCH予測
- Week 6: 統合分析
- Week 7: GRU深層学習予測

**変更点**:
- 自分のポートフォリオデータを使った分析
- Claudeによるリアルタイム解説

---

## 🛠️ 必要な機能実装

### 1. ポートフォリオトラッキング

**ファイル**: `src/portfolio/tracker.py`

```python
class PortfolioTracker:
    def __init__(self, api_key, api_secret):
        self.exchange = ccxt.mexc({
            'apiKey': api_key,
            'secret': api_secret,
        })

    def fetch_holdings(self):
        """保有資産を取得"""
        balance = self.exchange.fetch_balance()
        return balance

    def save_snapshot(self):
        """スナップショット保存（1分ごと）"""
        holdings = self.fetch_holdings()
        timestamp = datetime.now()

        # Parquetで保存
        df = pd.DataFrame([{
            'timestamp': timestamp,
            'symbol': symbol,
            'amount': amount,
            'value_usd': value,
            'price': price,
        } for symbol, amount, value, price in holdings])

        df.to_parquet(f'data/portfolio/snapshot_{timestamp}.parquet')

    def start_monitoring(self):
        """バックグラウンドで監視開始"""
        while True:
            self.save_snapshot()
            time.sleep(60)  # 1分ごと
```

### 2. Claude解説生成

**ファイル**: `src/analysis/live_commentary.py`

```python
def generate_commentary(portfolio_data, market_data):
    """
    入力:
    - portfolio_data: 保有資産データ
    - market_data: 市場データ（RSI, MACD等）

    出力:
    - markdown形式の解説ファイル
    """

    # Claude Codeに送信
    prompt = f"""
    ユーザーの保有状況:
    {portfolio_data}

    市場データ:
    {market_data}

    以下を含むわかりやすい解説を作成してください:
    1. 現状分析
    2. RSI/MACDの解釈
    3. 推奨アクション
    """

    # Claude APIで解説生成
    commentary = call_claude_api(prompt)

    # 保存
    with open(f'reports/commentary_{datetime.now()}.md', 'w') as f:
        f.write(commentary)
```

### 3. StreamlitUIにポートフォリオ表示

**ファイル**: `dashboard/portfolio_page.py`

```python
def show_portfolio():
    st.header("📊 マイ・ポートフォリオ")

    # 最新データ読み込み
    df = load_latest_portfolio()

    # 保有資産表示
    st.dataframe(df)

    # 損益グラフ
    fig = plot_profit_loss(df)
    st.plotly_chart(fig)

    # Claude解説表示
    latest_commentary = load_latest_commentary()
    st.markdown(latest_commentary)
```

---

## 🔐 MEXC API連携

### セキュリティ設定

**API Key作成手順**:

1. MEXC → アカウント → API管理
2. API Key作成
   - **読み取り専用**: ポートフォリオ確認のみ
   - **取引可能**: 自動売買する場合
   - **出金禁止**: 必ず設定

3. IPアドレス制限
   - 自宅IPのみ許可

4. 環境変数に保存
   ```bash
   export MEXC_API_KEY="your_key"
   export MEXC_API_SECRET="your_secret"
   ```

### 2FA問題の解決

**API Key使用後**:
- ✅ スマホ認証不要
- ✅ リアルタイム取引可能
- ✅ 自動化可能

---

## 📊 実装の優先順位

### Phase 1: 基盤（今すぐ）
1. [ ] Week 0カリキュラム作成
2. [ ] MEXC API Key設定ガイド作成
3. [ ] ポートフォリオトラッキング実装

### Phase 2: UI（次のフェーズ）
4. [ ] Streamlitにポートフォリオページ追加
5. [ ] リアルタイム損益グラフ
6. [ ] RSI/MACDスライダー操作

### Phase 3: Claude統合（その後）
7. [ ] リアルタイム解説生成
8. [ ] 動的カリキュラムシステム
9. [ ] インタラクティブ学習支援

---

## 📝 メモ

### 参考資料

- [MEXC API公式ドキュメント](https://mexcdevelop.github.io/apidocs/spot_v3_en/)
- [ccxt ライブラリ](https://github.com/ccxt/ccxt)
- [Streamlit公式](https://streamlit.io/)

### 次のステップ

1. データ収集完了を待つ
2. Week 0カリキュラムを作成
3. MEXC API連携ガイドを作成
4. ポートフォリオトラッキング実装開始

---

**最終更新**: 2025-11-01
**作成者**: Claude Code（with tatut）
